function First_Function() {    //Defining a function and naming it
    var sentence=" JavaScript is really confusing me,";    //Defining a variable and giving it a string value     
    sentence+=" but I'll keep studying!";                  //Defining a variable and concatenating it with the variable above
    document.getElementById("Concatenate").innerHTML=sentence;    //Putting the value of result into HTML element with "Concatenate" id
}
